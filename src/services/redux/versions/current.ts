/**
 * This should point to the current version
 *
 * To create a migration, copy the last 'version-X.ts' file
 * into a new 'version-Y.ts' file and increase the version by one
 *
 * E.g. 'cp version-0.ts version-1.ts'
 *
 * Now make your changes in 'version-1.ts'
 * and create the migration in the 'migrations.ts' file
 */

export * from './version-1'
